<!DOCTYPE html>
    <html>
        <head>
            <meta charset="utf-8">
            <title>Express Bar Index</title>
            <style type="text/css">
                body {
                  color: blanchedalmond;
                  background-color: steelblue;
                }
              </style>
        </head>
        <body>
            <!--

            {% for fruit in fruits%}
                <li>
                    <a href="/fruits/{{forloop.index0 }}">{{fruit.name}}</a>
                </li>
            {% endfor%}


            {% if fruit.readyToEat == true %}
                <p>It is ready to eat</p>
            {% else %}
                <p>It is NOT ready to eat</p>
            {% endif %}
            <a href="/fruits/">back to index</a>

        -->
        <h1>Welcome to gitPub</h1>


        <ul>
            {% for drink in drinks%}
                <li>
                    {% assign words = drink.name | split: ' ' %}

                    {% capture titlecase %}
                    {% for word in words %}
                        {{ word | capitalize }}
                    {% endfor %}{% endcapture %}
                    <a href="/drinks/{{forloop.index0}}">{{ titlecase }}
                </li>
            {% endfor %}
        </ul>
        </body>
    </html> 